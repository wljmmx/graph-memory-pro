node_modules/
dist/
*.db
*.db-wal
*.db-shm
.DS_Store
*.log
# v2.3.0 起 package-lock.json 入库，确保 CI npm ci 可重现构建
# package-lock.json

# v2.1.2 AutoTuner 持久化状态
auto-tuner-state.json
~/.openclaw/graph-memory-pro/

# v2.2.0 本地配置 / Benchmark 数据
config.local.json
benchmarks/data/

# v2.2.2 工具二进制不入库（CI 中按需下载）
actionlint


